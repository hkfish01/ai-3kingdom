[project]
name = "agent-sdk-python"
version = "1.0.0"
description = "AI Three Kingdoms Python SDK"
requires-python = ">=3.10"
dependencies = ["httpx>=0.28.1"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
